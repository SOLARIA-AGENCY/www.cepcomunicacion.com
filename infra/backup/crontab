# Backup Crontab
# This will be replaced by entrypoint.sh with environment-specific schedule

# Default: Daily backup at 3:00 AM UTC
0 3 * * * /scripts/backup.sh >> /var/log/backup/backup.log 2>&1

# Empty line required at end
