{
  "name": "@cepcomunicacion/cms",
  "version": "2.0.0",
  "description": "Payload CMS backend for CEPComunicacion v2",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx watch src/server.ts",
    "build": "tsc && copyfiles -u 1 'src/**/*.{json,css}' dist/",
    "start": "cross-env NODE_ENV=production node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "payload": "payload",
    "generate:types": "payload generate:types"
  },
  "dependencies": {
    "payload": "^3.0.0",
    "@payloadcms/db-postgres": "^3.0.0",
    "@payloadcms/richtext-lexical": "^3.0.0",
    "@payloadcms/storage-s3": "^3.0.0",
    "express": "^4.21.2",
    "dotenv": "^16.4.7",
    "zod": "^3.24.1",
    "bullmq": "^5.30.3",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "cross-env": "^7.0.3",
    "tsx": "^4.20.6",
    "copyfiles": "^2.4.1",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.2"
  }
}
